package time

import "time"

// get all times in 15 minute intervals

func getRaflaamoTimes() {
	currentTimeInUnix := time.Now().Unix()
	collectedRaflaamoTimes := make([]int64, 0, 96)
	for hour := 0; hour < 24; hour++ {

	}
}
